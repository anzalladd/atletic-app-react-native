import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  slide: {
    alignItems: 'center',
    paddingTop: 80,
  },
  center: {
    textAlign: 'center',
  },
  textHeader: {
    marginTop: 40,
  },
  textContent: {
    marginTop: 10,
  },
});

export default styles;
